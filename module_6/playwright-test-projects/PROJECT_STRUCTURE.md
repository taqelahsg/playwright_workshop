# Project Structure Visualization

## 📁 Directory Tree

```
playwright-test-projects/
│
├── 📄 Configuration Files
│   ├── playwright.config.ts      # Multi-project configuration
│   ├── package.json               # Dependencies and scripts
│   ├── .gitignore                 # Git ignore rules
│   └── tsconfig.json              # TypeScript config (auto-generated)
│
├── 📚 Documentation
│   ├── README.md                  # Main documentation
│   ├── QUICK_REFERENCE.md         # Command cheat sheet
│   └── PROJECT_STRUCTURE.md       # This file
│
├── 🧪 tests/
│   │
│   ├── 🔧 Setup & Teardown
│   │   ├── global.setup.ts        # Setup project (runs first)
│   │   └── global.cleanup.ts      # Cleanup project (runs last)
│   │
│   ├── 🌐 General Tests (all browsers)
│   │   ├── example.spec.ts        # Basic cross-browser tests
│   │   ├── example.smoke.spec.ts  # Smoke tests
│   │   └── example.viewport.spec.ts # Viewport tests
│   │
│   ├── 🔐 authenticated/
│   │   └── dashboard.spec.ts      # Authenticated user tests
│   │
│   ├── 👤 guest/
│   │   └── public.spec.ts         # Guest/unauthenticated tests
│   │
│   ├── 🔌 api/
│   │   └── api-example.spec.ts    # API tests (no browser)
│   │
│   └── 🎨 ui/
│       └── ui-example.spec.ts     # UI-focused tests
│
├── 🔐 playwright/.auth/
│   └── user.json                  # Generated auth state (gitignored)
│
├── 📊 test-results/               # Test outputs (gitignored)
├── 📈 playwright-report/          # HTML reports (gitignored)
└── 📦 node_modules/               # Dependencies (gitignored)
```

## 🔄 Project Execution Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    Test Execution Flow                      │
└─────────────────────────────────────────────────────────────┘

STAGE 1: SETUP
┌──────────────┐
│    setup     │  Creates authentication state
│  (1 worker)  │  Prepares test data
└──────┬───────┘  Verifies environment
       │
       ├─ global.setup.ts
       └─ Creates: playwright/.auth/user.json
       │
       ▼

STAGE 2: PARALLEL TEST EXECUTION
┌────────────────────────────────────────────────────────────┐
│  All projects run in parallel (depends on worker count)    │
└────────────────────────────────────────────────────────────┘

┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  chromium   │  │   firefox   │  │   webkit    │
│  (worker 1) │  │  (worker 2) │  │  (worker 3) │
└─────────────┘  └─────────────┘  └─────────────┘

┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│mobile-chrome│  │mobile-safari│  │ tablet-ipad │
│  (worker 4) │  │  (worker 5) │  │  (worker 1) │
└─────────────┘  └─────────────┘  └─────────────┘

┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│    smoke    │  │authenticated│  │    guest    │
│  (worker 2) │  │  (worker 3) │  │  (worker 4) │
└─────────────┘  └─────────────┘  └─────────────┘

┌─────────────┐  ┌─────────────┐
│     api     │  │  ui-chrome  │
│  (worker 5) │  │  (worker 1) │
└─────────────┘  └─────────────┘

       │
       │  (All tests complete)
       │
       ▼

STAGE 3: CLEANUP
┌──────────────┐
│   cleanup    │  Removes auth state
│  (1 worker)  │  Cleans temp files
└──────┬───────┘  Logs summary
       │
       ├─ global.cleanup.ts
       └─ Removes: playwright/.auth/
       │
       ▼
    ✅ Done!
```

## 🎯 Project Dependencies

```
                    ┌─────────┐
                    │  setup  │
                    └────┬────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ┌─────────┐    ┌─────────┐    ┌─────────┐
    │chromium │    │ firefox │    │ webkit  │
    └────┬────┘    └────┬────┘    └────┬────┘
         │              │              │
         │    ┌─────────┼──────────┐   │
         │    │         │          │   │
         ▼    ▼         ▼          ▼   ▼
    ┌────────────────────────────────────┐
    │  mobile-chrome, mobile-safari,     │
    │  authenticated, guest, smoke,      │
    │  api, ui-chrome, etc.              │
    └───────────────┬────────────────────┘
                    │
                    ▼
              ┌──────────┐
              │ cleanup  │
              └──────────┘
```

## 📋 Test File to Project Mapping

```
┌─────────────────────────────────┬────────────────────────────────┐
│         Test File               │      Runs on Projects          │
├─────────────────────────────────┼────────────────────────────────┤
│ example.spec.ts                 │ All browser projects           │
│                                 │ (chromium, firefox, webkit,    │
│                                 │  mobile-chrome, mobile-safari, │
│                                 │  tablet-ipad, Google Chrome,   │
│                                 │  Microsoft Edge)               │
├─────────────────────────────────┼────────────────────────────────┤
│ example.smoke.spec.ts           │ smoke                          │
├─────────────────────────────────┼────────────────────────────────┤
│ example.viewport.spec.ts        │ desktop-1920, desktop-1366     │
├─────────────────────────────────┼────────────────────────────────┤
│ global.setup.ts                 │ setup                          │
├─────────────────────────────────┼────────────────────────────────┤
│ global.cleanup.ts               │ cleanup                        │
├─────────────────────────────────┼────────────────────────────────┤
│ authenticated/dashboard.spec.ts │ authenticated                  │
├─────────────────────────────────┼────────────────────────────────┤
│ guest/public.spec.ts            │ guest                          │
├─────────────────────────────────┼────────────────────────────────┤
│ api/api-example.spec.ts         │ api                            │
├─────────────────────────────────┼────────────────────────────────┤
│ ui/ui-example.spec.ts           │ ui-chrome                      │
└─────────────────────────────────┴────────────────────────────────┘
```

## 🔧 Configuration Breakdown

### playwright.config.ts

```typescript
┌─────────────────────────────────────────┐
│         Global Settings                 │
├─────────────────────────────────────────┤
│ testDir: './tests'                      │
│ timeout: 30000                          │
│ fullyParallel: true                     │
│ retries: CI ? 2 : 0                     │
│ workers: CI ? 1 : undefined             │
│ reporter: html, list, json              │
│ baseURL: 'https://playwright.dev'       │
│ trace: 'on-first-retry'                 │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│         Project Configurations          │
├─────────────────────────────────────────┤
│ • setup                                 │
│ • chromium, firefox, webkit             │
│ • Google Chrome, Microsoft Edge         │
│ • mobile-chrome, mobile-safari          │
│ • tablet-ipad                           │
│ • smoke                                 │
│ • authenticated, guest                  │
│ • api, ui-chrome                        │
│ • desktop-1920, desktop-1366            │
│ • cleanup                               │
└─────────────────────────────────────────┘
```

### Project Configuration Elements

```
Each Project Can Configure:
┌────────────────────────────┐
│ name          (required)   │ Project identifier
│ use           (optional)   │ Browser/device settings
│ dependencies  (optional)   │ Other projects to run first
│ testMatch     (optional)   │ File patterns to include
│ testIgnore    (optional)   │ File patterns to exclude
│ testDir       (optional)   │ Directory for this project
│ timeout       (optional)   │ Test timeout override
│ retries       (optional)   │ Retry count override
│ grep          (optional)   │ Test title filter
└────────────────────────────┘
```

## 🗂️ File Purposes

```
┌─────────────────────────────────────────────────────────────┐
│ Configuration Files                                         │
├─────────────────────────────┬───────────────────────────────┤
│ playwright.config.ts        │ Main Playwright configuration │
│ package.json                │ NPM scripts & dependencies    │
│ .gitignore                  │ Ignored files/directories     │
└─────────────────────────────┴───────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Documentation Files                                         │
├─────────────────────────────┬───────────────────────────────┤
│ README.md                   │ Full project documentation    │
│ QUICK_REFERENCE.md          │ Command cheat sheet           │
│ PROJECT_STRUCTURE.md        │ Structure visualization       │
└─────────────────────────────┴───────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Test Files                                                  │
├─────────────────────────────┬───────────────────────────────┤
│ *.spec.ts                   │ Test specifications           │
│ *.setup.ts                  │ Setup configurations          │
│ *.cleanup.ts                │ Cleanup/teardown              │
└─────────────────────────────┴───────────────────────────────┘
```

## 🎭 Browser & Device Matrix

```
Desktop Browsers:
┌─────────────┬──────────────┬──────────────┐
│  Chromium   │   Firefox    │   WebKit     │
│   (Open)    │    (Open)    │    (Open)    │
└─────────────┴──────────────┴──────────────┘

Branded Browsers:
┌─────────────┬──────────────┐
│Google Chrome│Microsoft Edge│
│  (Branded)  │  (Branded)   │
└─────────────┴──────────────┘

Mobile Devices:
┌─────────────┬──────────────┐
│  Pixel 5    │  iPhone 12   │
│  (Android)  │    (iOS)     │
└─────────────┴──────────────┘

Tablets:
┌─────────────┐
│  iPad Pro   │
│    (iOS)    │
└─────────────┘

Custom Viewports:
┌─────────────┬──────────────┐
│  1920x1080  │  1366x768    │
│   (Full HD) │  (Standard)  │
└─────────────┴──────────────┘
```

## 📊 Test Categories

```
By Authentication:
┌──────────────┬──────────────┐
│Authenticated │    Guest     │
│ (with auth)  │  (no auth)   │
└──────────────┴──────────────┘

By Type:
┌──────────────┬──────────────┐
│     API      │      UI      │
│ (no browser) │  (browser)   │
└──────────────┴──────────────┘

By Purpose:
┌──────────────┬──────────────┬──────────────┐
│    Smoke     │  Regression  │   Viewport   │
│   (fast)     │   (full)     │  (layouts)   │
└──────────────┴──────────────┴──────────────┘
```

## 🚀 Common Workflows

### Development Workflow
```
1. Write test → 2. Run on chromium → 3. Debug → 4. Fix → 5. Re-run
   │               │                    │        │         │
   └───────────────┴────────────────────┴────────┴─────────┘
                Fast feedback loop
```

### CI/CD Workflow
```
1. Commit → 2. CI runs setup → 3. All projects parallel → 4. Cleanup → 5. Report
   │           │                  │                         │            │
   └───────────┴──────────────────┴─────────────────────────┴────────────┘
              Complete validation
```

### Smoke Test Workflow
```
1. Deploy → 2. Run smoke tests → 3. All critical paths verified → 4. Success
   │           │                   │                              │
   └───────────┴───────────────────┴──────────────────────────────┘
         Quick health check
```

---

This visualization helps understand how the project is organized and how different components interact with each other.
